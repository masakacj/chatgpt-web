name: Unified ChatGPT Userscript

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate-and-package:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate
        run: npm run check

      - name: Package
        run: |
          set -euo pipefail
          rm -rf dist
          mkdir -p dist
          cp safari/chatgpt-safari.user.js dist/ChatGPT-Web-Unified.user.js
          (
            cd dist
            sha256sum ChatGPT-Web-Unified.user.js > ChatGPT-Web-Unified.user.js.sha256
            zip -q ChatGPT-Web-Unified.zip ChatGPT-Web-Unified.user.js
            sha256sum ChatGPT-Web-Unified.zip > ChatGPT-Web-Unified.zip.sha256
          )

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ChatGPT-Web-Unified
          path: dist/*
          if-no-files-found: error

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            TAG="$GITHUB_REF_NAME"
          else
            VERSION="$(node -p "require('./package.json').version")"
            TAG="v$VERSION"
          fi

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" dist/* --clobber
            gh release edit "$TAG" \
              --title "ChatGPT Web Unified $TAG" \
              --target "$GITHUB_SHA"
          else
            gh release create "$TAG" dist/* \
              --target "$GITHUB_SHA" \
              --title "ChatGPT Web Unified $TAG" \
              --generate-notes
          fi
